{
  "name": "chat-bot",
  "version": "0.0.1",
  "description": "PolloPollo chat-bot used to integrate https://pollopollo.org with the Obyte technology",
  "main": "lib/index.js",
  "scripts": {
    "kill": "ps aux | grep '[y]arn start' | awk '{print $2}' | xargs kill -9 && ps aux | grep '[y]arn dev ' | awk '{print $2}' | xargs kill -9 && ps aux | grep '[n]ode --max-old-space-size=4096' | awk '{print $2}' | xargs kill -9",
    "start": "node --max-old-space-size=4096 lib/index.js",
    "dev": "yarn compile && yarn start",
    "devBash": "yarn kill && yarn dev",
    "documentation": "rimraf documentation && typedoc --out ./documentation --tsconfig tsconfig.json --hideGenerator --name PolloPolloChatBot",
    "lint": "tslint --project tsconfig.json --config tslint.json --format stylish",
    "compile": "rimraf lib && tsc && yarn lint --fix && copyfiles conf.js ./lib && copyfiles package.json ./lib"
  },
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.4",
    "headless-obyte": "git+https://github.com/byteball/headless-obyte.git",
    "mariadb": "^2.0.3",
    "node-cron": "^2.0.3",
    "node-fetch": "^2.3.0",
    "ocore": "git+https://github.com/byteball/ocore.git",
    "proper-lockfile": "^4.1.1",
    "ssl-root-cas": "^1.3.1"
  },
  "devDependencies": {
    "@types/express": "^4.16.1",
    "@types/node": "^11.13.7",
    "@types/node-cron": "^2.0.2",
    "@types/node-fetch": "^2.3.2",
    "@types/proper-lockfile": "^3.0.1",
    "chalk": "^2.4.2",
    "copyfiles": "^2.1.0",
    "husky": "^1.3.1",
    "rimraf": "^2.6.3",
    "tslint": "^5.16.0",
    "tslint-microsoft-contrib": "^6.1.1",
    "typedoc": "^0.14.2",
    "typescript": "^3.4.5",
    "yarn": "^1.15.2"
  }
}
